<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!--
      Three permissions declared here, all optional and all explained in the interface
      before they are requested.

      Note what is absent: INTERNET. The app has no server and cannot make a network
      request even if a future bug tried to, which is a stronger guarantee than any
      privacy policy. WorkManager's own manifest does contribute ACCESS_NETWORK_STATE,
      WAKE_LOCK, RECEIVE_BOOT_COMPLETED and FOREGROUND_SERVICE to the merged manifest;
      they are how it survives reboots and Doze so the booster schedule still reaches
      the user in eleven months. Without INTERNET, ACCESS_NETWORK_STATE can observe
      connectivity but nothing can be sent anywhere. We are not stripping them, because
      removing a permission a library declares is how you get a SecurityException in
      production; we are documenting them instead.
    -->
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.VIBRATE" />

    <!-- The camera is used only for fingertip pulse during a breathing session. -->
    <uses-feature
        android:name="android.hardware.camera"
        android:required="false" />
    <uses-feature
        android:name="android.hardware.camera.flash"
        android:required="false" />

    <application
        android:name=".HugMunApplication"
        android:allowBackup="false"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher"
        android:supportsRtl="true"
        android:theme="@style/Theme.HugMun.Starting"
        tools:targetApi="34">

        <activity
            android:name=".MainActivity"
            android:configChanges="orientation|screenSize|screenLayout|keyboardHidden|uiMode|density|fontScale"
            android:exported="true"
            android:launchMode="singleTask"
            android:screenOrientation="portrait"
            android:theme="@style/Theme.HugMun.Starting"
            android:windowSoftInputMode="adjustResize"
            tools:ignore="DiscouragedApi,LockedOrientationActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!--
          No exported components beyond the launcher activity. Nothing else in the system
          has any reason to reach into this app's data.
        -->
        <provider
            android:name="androidx.startup.InitializationProvider"
            android:authorities="${applicationId}.androidx-startup"
            android:exported="false"
            tools:node="merge" />

    </application>

</manifest>
