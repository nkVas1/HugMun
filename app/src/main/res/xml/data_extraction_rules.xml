<?xml version="1.0" encoding="utf-8"?>
<!--
  Nothing leaves the device.

  The database holds photographs and names of the user's family, mood entries, blood
  pressure readings and raw pulse data. Cloud backup and device-to-device transfer are
  both disabled: the user can produce an export deliberately, and that is the only way
  this data is ever meant to move.
-->
<data-extraction-rules>
    <cloud-backup>
        <exclude domain="root" />
        <exclude domain="database" />
        <exclude domain="sharedpref" />
        <exclude domain="file" />
        <exclude domain="external" />
    </cloud-backup>
    <device-transfer>
        <exclude domain="root" />
        <exclude domain="database" />
        <exclude domain="sharedpref" />
        <exclude domain="file" />
        <exclude domain="external" />
    </device-transfer>
</data-extraction-rules>
